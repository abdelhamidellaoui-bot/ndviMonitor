% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_growth_stage.R
\name{detect_growth_stage}
\alias{detect_growth_stage}
\title{Détecter les stades phénologiques simples (croissance, pic, sénescence)}
\usage{
detect_growth_stage(
  ndvi_table,
  value_col = NULL,
  growth_threshold = 0.3,
  senescence_drop = 0.1
)
}
\arguments{
\item{ndvi_table}{\code{data.frame} issu de \code{\link[=extract_ndvi]{extract_ndvi()}} ou
\code{\link[=smooth_timeseries]{smooth_timeseries()}}.}

\item{value_col}{Colonne de NDVI à utiliser. Par défaut, \code{"smoothed"}
si elle existe, sinon \code{"mean"}.}

\item{growth_threshold}{Seuil de NDVI au-dessus duquel la croissance
est considérée comme commencée. Par défaut \code{0.3}.}

\item{senescence_drop}{Baisse relative de NDVI par rapport au pic
(fraction, ex. \code{0.10} pour -10 \%) à partir de laquelle la
sénescence est déclarée. Par défaut \code{0.10}.}
}
\value{
Un \code{data.frame} avec une ligne par parcelle et les colonnes
\code{parcel_id}, \code{start_growth_date}, \code{peak_date}, \code{peak_ndvi},
\code{senescence_date}.
}
\description{
Identifie, pour chaque parcelle, trois stades phénologiques simples à
partir d'une série temporelle de NDVI (de préférence lissée) : le
début de croissance (premier dépassement d'un seuil NDVI), le pic
végétatif (valeur maximale) et le début de sénescence (première baisse
marquée après le pic).
}
\examples{
ndvi_table <- ndvi_demo_table()
detect_growth_stage(ndvi_table, value_col = "mean")
}
