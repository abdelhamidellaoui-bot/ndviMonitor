% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_sentinel.R
\name{download_sentinel}
\alias{download_sentinel}
\title{Télécharger des images Sentinel-2 (bandes Rouge et NIR)}
\usage{
download_sentinel(
  parcels,
  start_date,
  end_date,
  max_cloud_cover = 20,
  dest_dir = "sentinel_data",
  collection = "sentinel-2-l2a",
  max_items = 10
)
}
\arguments{
\item{parcels}{Objet \link[sf:sf]{sf::sf} représentant l'emprise d'étude (sortie
de \code{\link[=import_parcels]{import_parcels()}}).}

\item{start_date, end_date}{Dates de début et de fin
(\code{"AAAA-MM-JJ"}).}

\item{max_cloud_cover}{Pourcentage maximal de couverture nuageuse
accepté (0-100). Par défaut \code{20}.}

\item{dest_dir}{Dossier où enregistrer les bandes téléchargées. Créé
si nécessaire. Par défaut \code{"sentinel_data"}.}

\item{collection}{Nom de la collection STAC. Par défaut
\code{"sentinel-2-l2a"}.}

\item{max_items}{Nombre maximal de scènes recherchées. Par défaut
\code{10}.}
}
\value{
Une liste (invisible) contenant \verb{$items} (objet STAC retourné
par \pkg{rstac}, filtré sur la couverture nuageuse) et \verb{$files}
(chemins des fichiers Rouge / NIR téléchargés localement).
}
\description{
Interroge le catalogue STAC public \strong{Microsoft Planetary Computer}
pour récupérer les scènes Sentinel-2 Niveau 2A (réflectance de
surface) couvrant l'emprise des parcelles agricoles fournies, sur une
période donnée, puis télécharge les bandes Rouge (B04) et Proche
Infrarouge (B08) nécessaires au calcul du NDVI.
}
\details{
Microsoft Planetary Computer est un catalogue STAC public, gratuit et
ne nécessitant pas de clé d'API pour la recherche ; un jeton de
signature est obtenu automatiquement pour le téléchargement des
fichiers via \code{\link[rstac:sign_planetary_computer]{rstac::sign_planetary_computer()}}. Cette fonction
nécessite le package \pkg{rstac} (voir \code{Suggests}) ainsi qu'un accès
Internet.
}
\examples{
\dontrun{
parcels <- import_parcels("parcelles.geojson", plot = FALSE)
sentinel <- download_sentinel(
  parcels,
  start_date = "2024-03-01", end_date = "2024-07-01",
  max_cloud_cover = 20, dest_dir = "sentinel_data"
)
sentinel$files
}
}
