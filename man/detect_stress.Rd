% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_stress.R
\name{detect_stress}
\alias{detect_stress}
\title{Détecter des anomalies / stress simples de végétation}
\usage{
detect_stress(
  ndvi_table,
  value_col = NULL,
  drop_threshold = 0.2,
  low_growth_threshold = -0.02
)
}
\arguments{
\item{ndvi_table}{\code{data.frame} issu de \code{\link[=extract_ndvi]{extract_ndvi()}} ou
\code{\link[=smooth_timeseries]{smooth_timeseries()}}.}

\item{value_col}{Colonne de NDVI à utiliser. Par défaut, \code{"smoothed"}
si elle existe, sinon \code{"mean"}.}

\item{drop_threshold}{Baisse relative par rapport à la moyenne
historique de la parcelle (fraction) à partir de laquelle une
alerte \code{"baisse_brutale"} est déclenchée. Par défaut \code{0.20}
(-20 \%).}

\item{low_growth_threshold}{Pente NDVI minimale (différence entre
deux dates consécutives) en-dessous de laquelle une alerte
\code{"faible_croissance"} est déclenchée. Par défaut \code{-0.02}.}
}
\value{
Le \code{data.frame} d'entrée complété des colonnes
\code{historical_mean}, \code{pct_deviation}, \code{alert_type}
(\code{"baisse_brutale"}, \code{"faible_croissance"} ou \code{"normal"}) et
\code{alert} (logique).
}
\description{
Compare, pour chaque date et chaque parcelle, le NDVI observé à la
moyenne historique de la parcelle (toutes dates confondues) et
déclenche une alerte lorsque la baisse dépasse un seuil relatif, ou
lorsque la pente du NDVI entre deux dates consécutives est anormalement
faible (faible croissance).
}
\examples{
ndvi_table <- ndvi_demo_table()
detect_stress(ndvi_table, value_col = "mean")
}
