% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smooth_timeseries.R
\name{smooth_timeseries}
\alias{smooth_timeseries}
\title{Lisser une série temporelle NDVI}
\usage{
smooth_timeseries(
  ndvi_table,
  method = c("moving_average", "loess"),
  value_col = "mean",
  window = 3,
  span = 0.5
)
}
\arguments{
\item{ndvi_table}{\code{data.frame} issu de \code{\link[=extract_ndvi]{extract_ndvi()}}, avec au
minimum les colonnes \code{parcel_id}, \code{date} et une colonne de valeur
(\code{value_col}).}

\item{method}{Méthode de lissage : \code{"moving_average"} (défaut) ou
\code{"loess"}.}

\item{value_col}{Nom de la colonne à lisser. Par défaut \code{"mean"}.}

\item{window}{Taille de la fenêtre (nombre de points, impair de
préférence) pour la moyenne mobile. Par défaut \code{3}.}

\item{span}{Paramètre \code{span} du LOESS (0-1, plus la valeur est
grande, plus le lissage est fort). Par défaut \code{0.5}.}
}
\value{
Le \code{data.frame} d'entrée complété d'une colonne \code{smoothed}.
}
\description{
Applique un lissage par moyenne mobile ou par régression locale
(LOESS) à une série temporelle de NDVI, parcelle par parcelle, afin de
réduire le bruit (nuages résiduels, ombres, bruit capteur).
}
\examples{
ndvi_table <- ndvi_demo_table()
smooth_timeseries(ndvi_table, method = "moving_average")
}
