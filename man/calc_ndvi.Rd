% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_ndvi.R
\name{calc_ndvi}
\alias{calc_ndvi}
\title{Calculer le NDVI à partir des bandes Rouge et NIR}
\usage{
calc_ndvi(red_files, nir_files, dates = NULL, clamp = TRUE)
}
\arguments{
\item{red_files, nir_files}{Vecteurs de chemins vers les fichiers
raster (ou objets \link[terra:SpatRaster-class]{terra::SpatRaster} déjà chargés) des bandes
Rouge et NIR, dans le même ordre, une paire par date.}

\item{dates}{Vecteur de dates (\code{Date} ou caractère \code{"AAAA-MM-JJ"})
associées à chaque paire d'images. Si \code{NULL}, des étiquettes
factices \code{"D1"}, \code{"D2"}, ... sont utilisées.}

\item{clamp}{Logique. Si \code{TRUE} (par défaut), les valeurs de NDVI
sont bornées dans \verb{[-1, 1]}.}
}
\value{
Un objet \link[terra:SpatRaster-class]{terra::SpatRaster} multi-couches (une couche par
date), nommé d'après \code{dates}.
}
\description{
Calcule le Normalized Difference Vegetation Index (NDVI) :
\deqn{NDVI = \frac{NIR - Red}{NIR + Red}}
pour une ou plusieurs paires d'images Rouge / Proche Infrarouge, et
renvoie un empilement (stack) temporel \link[terra:SpatRaster-class]{terra::SpatRaster}, une
couche par date.
}
\examples{
ndvi_stack <- ndvi_demo_stack()
ndvi_stack
}
