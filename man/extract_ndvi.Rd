% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_ndvi.R
\name{extract_ndvi}
\alias{extract_ndvi}
\title{Extraire les statistiques NDVI par parcelle}
\usage{
extract_ndvi(ndvi_stack, parcels, stats = c("mean", "median"))
}
\arguments{
\item{ndvi_stack}{Objet \link[terra:SpatRaster-class]{terra::SpatRaster} multi-couches (sortie de
\code{\link[=calc_ndvi]{calc_ndvi()}}), une couche par date.}

\item{parcels}{Objet \link[sf:sf]{sf::sf} des parcelles (sortie de
\code{\link[=import_parcels]{import_parcels()}}). Doit contenir une colonne \code{parcel_id}.}

\item{stats}{Statistiques à calculer parmi \code{"mean"} et \code{"median"}.
Par défaut, les deux.}
}
\value{
Un \code{data.frame} avec les colonnes \code{parcel_id}, \code{date}, et
\code{mean} et/ou \code{median}.
}
\description{
Calcule des statistiques zonales (moyenne et/ou médiane) du NDVI pour
chaque parcelle agricole et pour chaque date d'un empilement temporel,
et renvoie un tableau "long" (une ligne par parcelle x date) prêt pour
l'analyse de séries temporelles.
}
\examples{
ndvi_stack <- ndvi_demo_stack()
extract_ndvi(ndvi_stack, demo_parcels)
}
