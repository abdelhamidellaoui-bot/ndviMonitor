% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ndvi_curve.R
\name{plot_ndvi_curve}
\alias{plot_ndvi_curve}
\title{Tracer les courbes d'évolution du NDVI}
\usage{
plot_ndvi_curve(
  ndvi_table,
  value_col = "mean",
  smoothed_col = "smoothed",
  stages = NULL,
  parcels_subset = NULL,
  title = "Evolution temporelle du NDVI"
)
}
\arguments{
\item{ndvi_table}{\code{data.frame} issu de \code{\link[=extract_ndvi]{extract_ndvi()}} ou
\code{\link[=smooth_timeseries]{smooth_timeseries()}}.}

\item{value_col}{Colonne de NDVI brute à tracer (points). Par défaut
\code{"mean"}.}

\item{smoothed_col}{Colonne de NDVI lissée à superposer (ligne), si
présente dans \code{ndvi_table}. Par défaut \code{"smoothed"}. Utiliser
\code{NULL} pour ne pas l'afficher.}

\item{stages}{\code{data.frame} optionnel issu de
\code{\link[=detect_growth_stage]{detect_growth_stage()}}, pour superposer les dates de pic
végétatif.}

\item{parcels_subset}{Vecteur optionnel d'identifiants de parcelles à
afficher. Par défaut, toutes les parcelles sont affichées.}

\item{title}{Titre du graphique.}
}
\value{
Un objet \code{ggplot} (affiché et renvoyé de manière invisible).
}
\description{
Trace l'évolution temporelle du NDVI pour une ou plusieurs parcelles,
avec en option l'affichage de la courbe lissée et des dates de pic
végétatif détectées.
}
\examples{
ndvi_table <- smooth_timeseries(ndvi_demo_table())
plot_ndvi_curve(ndvi_table)
}
