% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_report.R
\name{generate_report}
\alias{generate_report}
\title{Générer un rapport automatique HTML}
\usage{
generate_report(
  ndvi_table,
  stages,
  ndvi_stack = NULL,
  parcels = NULL,
  output_file = "ndvi_report.html",
  title = "Rapport de suivi NDVI"
)
}
\arguments{
\item{ndvi_table}{\code{data.frame} issu de \code{\link[=extract_ndvi]{extract_ndvi()}},
\code{\link[=smooth_timeseries]{smooth_timeseries()}} et/ou \code{\link[=detect_stress]{detect_stress()}}.}

\item{stages}{\code{data.frame} issu de \code{\link[=detect_growth_stage]{detect_growth_stage()}}.}

\item{ndvi_stack}{Objet \link[terra:SpatRaster-class]{terra::SpatRaster} optionnel, pour inclure
une carte NDVI dans le rapport.}

\item{parcels}{Objet \link[sf:sf]{sf::sf} optionnel, superposé à la carte.}

\item{output_file}{Chemin du fichier de sortie (\code{.html}). Par défaut
\code{"ndvi_report.html"} dans le répertoire courant.}

\item{title}{Titre du rapport.}
}
\value{
Le chemin (caractère) du rapport généré, de manière
invisible.
}
\description{
Compile un rapport HTML (R Markdown) résumant l'analyse NDVI d'un
ensemble de parcelles : statistiques générales, courbes temporelles,
carte du dernier NDVI disponible, stades phénologiques détectés et
alertes de stress.
}
\examples{
\dontrun{
ndvi_table <- smooth_timeseries(ndvi_demo_table())
stages <- detect_growth_stage(ndvi_table)
generate_report(ndvi_table, stages, output_file = tempfile(fileext = ".html"))
}
}
