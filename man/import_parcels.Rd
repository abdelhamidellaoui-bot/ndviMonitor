% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_parcels.R
\name{import_parcels}
\alias{import_parcels}
\title{Importer des parcelles agricoles}
\usage{
import_parcels(path, id_col = "parcel_id", target_crs = NULL, plot = TRUE)
}
\arguments{
\item{path}{Chemin vers le fichier vectoriel (\code{.shp}, \code{.geojson},
\code{.json}).}

\item{id_col}{Nom de la colonne identifiant chaque parcelle. Si la
colonne n'existe pas, un identifiant \code{parcel_id} est généré
automatiquement (\code{"P1"}, \code{"P2"}, ...).}

\item{target_crs}{Code EPSG (entier) vers lequel reprojeter les
parcelles. Si \code{NULL} (par défaut), le CRS d'origine est conservé.}

\item{plot}{Logique. Si \code{TRUE} (par défaut), un aperçu des parcelles
est affiché dans le périphérique graphique courant.}
}
\value{
Un objet \link[sf:sf]{sf::sf} contenant les parcelles, avec une colonne
\code{parcel_id}.
}
\description{
Lit un fichier vectoriel de parcelles agricoles (shapefile \code{.shp} ou
GeoJSON \code{.geojson} / \code{.json}), vérifie que son système de coordonnées
(CRS) est défini, ajoute un identifiant de parcelle si nécessaire et
affiche un aperçu graphique.
}
\examples{
parcels_path <- system.file("extdata", "demo_parcels.geojson",
                             package = "ndviMonitor")
parcels <- import_parcels(parcels_path, plot = FALSE)
parcels
}
